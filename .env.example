EMAIL_USER=seuemail@gmail.com
EMAIL_PASS=abcdefghijklmnop

# Renomeie este arquivo para `.env` antes de rodar a aplicação.
# Preencha os campos com suas informações reais.

# EMAIL_USER: Endereço de e-mail do remetente. Esse será o e-mail usado para enviar as mensagens do aplicativo.
# EMAIL_PASS: Senha do aplicativo (App Password), não é a senha normal da sua conta.
    # IMPORTANTE:
    # - Para contas Google (Gmail), é necessário ativar a verificação em duas etapas (2FA) na conta
    #   e depois gerar uma senha de app específica para usar aqui.
    #   Como gerar:
    #   1. Acesse: https://myaccount.google.com/apppasswords
    #   2. Faça login, selecione "Outro (nome personalizado)", por exemplo: "Meu App"
    #   3. Copie a senha gerada e cole aqui.
    # - Para outros provedores (Outlook, Yahoo, etc.), também pode ser necessário gerar uma senha de app,
    #   dependendo das configurações de segurança da conta.